## How This Works

System Prompt
+ Previous Messages
+ New Human Message
↓
LLM Generates Response
↓
Save AI Response
↓
Continue Conversation

"""So the model remembers the conversation because you resend the history every time."""



### Installation:

pip install -U langchain langchain-community langchain-core
pip install ollama
pip install langchain-ollama
pip install langchain-ollama --upgrade